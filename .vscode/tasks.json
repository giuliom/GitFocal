{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "create-package",
      "type": "shell",
      "command": "npx @vscode/vsce package -o build",
      "problemMatcher": [],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Package the extension into a VSIX under the build directory"
    },
    {
      "label": "create-package-deno",
      "type": "shell",
      "command": "deno run -A npm:@vscode/vsce package -o build --no-dependencies",
      "problemMatcher": [],
      "detail": "Package the extension into a VSIX under the build directory using Deno"
    }
  ]
}